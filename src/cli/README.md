# CLI layer

Argument parsing with `node:util`'s `parseArgs`, the command inventory that generates help,
completions and the man page, and the mapping from a result object to an exit status.

This layer may import every layer below it. Nothing imports it.

Empty today.
