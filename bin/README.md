# bin

The published executable lands here once the CLI layer exists, as a single bundled entry
file that `package.json` points `bin` at.

Empty today, deliberately: `package.json` declares no `bin`, so nothing can be installed
that does not work.
