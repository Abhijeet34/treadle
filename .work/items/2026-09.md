schema: 1

# field-edits: Field edits: set, estimate and assign, with one owner per field

type: story
state: ready
filed_at: 2026-09-04T22:00:51Z
version: 2
priority: 1
points: 5

## Description

The interface specification carves set so no field has two owners: set refuses points, hours and assignee, and estimate and assign own those. Section 7 records the disagreement, that a single set would be smaller, as a question for the captain.

## Acceptance criteria

- [ ] set writes one stored field and refuses the three it does not own
- [ ] estimate and assign write their own field
- [ ] each reports the before and after pair the event log stores
- [ ] a field that was already correct produces no set line and does not count

# relations: Relations: link and unlink, and the relation graph in the store

type: story
state: ready
filed_at: 2026-09-04T22:00:51Z
version: 2
priority: 1
points: 8

## Description

The domain layer has the whole relation model and the store persists none of it, so every blocker list this build computes is empty by construction. Guards G2 and G7, the derived blocked flag, the dep component of the next ranking and the blocked aggregate on a list all read as zero until this lands.

## Acceptance criteria

- [ ] a relation survives a round trip through the record grammar
- [ ] blockersOf returns the active blockers of a stored item
- [ ] G2 refuses a start that a blocker should refuse
- [ ] the next ranking dep component is non-zero for an item that blocks another

# sprints-boards: Sprints and boards as stored entities

type: story
state: draft
filed_at: 2026-09-04T22:00:51Z
version: 1
priority: 2
points: 13

## Description

Guard G3 reads a column and its work-in-progress limit and guard G4 reads iteration membership. This build hands the domain no board at all, which the domain reads as no limit, and iteration membership as always true. status omits sprint identity, day of sprint, committed and remaining points, and the WIP line, and absent_features says so on every invocation.

# undo: Undo: revert the most recent transaction by appending its inverse

type: story
state: ready
filed_at: 2026-09-04T22:00:52Z
version: 2
priority: 2
points: 5

## Description

Most recent in the workspace rather than yours, because a per-actor stack lets two people undo past each other. Not itself an undo, so repeated calls walk backwards. Appending rather than deleting, so the revert is in the history. Severe confirmation: the transaction id typed, or --confirm with it.

## Acceptance criteria

- [ ] repeated undo walks backwards rather than toggling
- [ ] a revert whose entities have moved since is refused and names the newer transaction
- [ ] the revert is itself a transaction in the history

# history: History, and resolving a transaction id a confirmation printed

type: story
state: draft
filed_at: 2026-09-04T22:00:52Z
version: 1
priority: 2
points: 3

## Description

R4 is only half met: every mutation prints its transaction id and no read verb resolves one. history --txn is the other half, and it is what ties a confirmation to the event it produced.

# gate-command: Gate: print what the ready or done gate will decide before you run a transition

type: task
state: ready
filed_at: 2026-09-04T22:00:52Z
version: 2
priority: 3
points: 3

## Description

explain already prints the failing rules of both gates. gate is the cheap single-gate form the transition help points at, and it is the one command that reports a verdict rather than a record.

# workspace-config: Workspace configuration, and the precedence a flag beats

type: story
state: draft
filed_at: 2026-09-04T22:00:52Z
version: 1
priority: 2
points: 5

## Description

Four defaults are compiled in and should be workspace configuration: which types have a review step, which is guard G5's input and is currently story, bug and epic; the next weights; the point scale; and the board columns. Precedence is flag, then environment, then workspace file, then built-in default, with the rendering deliberately excluded because DR5 refused an implicit input to it.

# doctor: Doctor, and the finding for a value shaped like a credential

type: story
state: draft
filed_at: 2026-09-04T22:00:52Z
version: 1
priority: 2
points: 8

## Description

Threat-model finding F10 has two halves and this build closed one. Verbose logging now reports a field by name and size. The other half is a doctor finding that flags a stored value matching a common secret shape, because a work-management tool is a place people paste things and the store is committed to git.

# metrics: Metrics: velocity, burndown and cumulative flow

type: story
state: draft
filed_at: 2026-09-04T22:00:53Z
version: 1
priority: 3
points: 13

## Description

Interface sections C.3 and C.4 give the layout algorithms and A.3 gives both budgets, which matter more here than anywhere else: cumulative_flow is 326 bytes and 211 Claude tokens, 1.55 bytes per token, so a byte budget alone mis-prices it by roughly two.

# human-layouts: The human rendering's board, charts and colour

type: story
state: draft
filed_at: 2026-09-04T22:00:53Z
version: 1
priority: 3
points: 8

## Description

This build ships a generic aligned projection with the global width rule and no colour at all. Interface section 4 gives byte-exact layouts and a six-assertion snapshot contract, one of which is the mechanical proof that colour carries no information: colour always output, with the escapes stripped, is byte-identical to colour never.

# export: Export to Markdown and CSV, with the formula guard

type: story
state: draft
filed_at: 2026-09-04T22:00:53Z
version: 1
priority: 4
points: 5

## Description

Threat-model finding F4: the CSV export is RFC 4180 quoted and not formula guarded, so a title beginning with an equals sign is a formula in a spreadsheet. RFC 4180 is the format and the guard is a prefix rule on top of it.

# hooks: Hooks, and the two exit codes the contract reserves for them

type: story
state: draft
filed_at: 2026-09-04T22:00:53Z
version: 1
priority: 4
points: 8

## Description

DR6 makes a hook an external executable that can veto a proposal and never edit it. DR5 reserves HOOK_REFUSED at 3 and HOOK_FAILED at 1, and neither is in this build's closed set because nothing can produce one. Threat-model findings F1 and F7 are the consent gate and the path safety rule that land with it.

# generated-artefacts: Completions, the man page, and the adapter write-safety contract

type: task
state: ready
filed_at: 2026-09-04T22:00:53Z
version: 2
priority: 4
points: 5

## Description

All three generate from the command inventory that already backs help and the schemas. Threat-model finding F11 is the one new rule: an adapter write prints its diff, takes a moderate confirmation, backs up what it replaces, is idempotent on a re-run, and states the command that reverts it.

# packaging: A bundled dist entry so the package can be published

type: chore
state: ready
filed_at: 2026-09-04T22:00:54Z
version: 2
priority: 4
points: 3

## Description

bin/treadle.js imports TypeScript directly and package.json is private. DR1 names esbuild at 8.7 milliseconds per invocation as the development dependency that makes a single published file. Nothing publishes until the name clearance in the README status table has run.

# supply-chain: Supply-chain controls F13 names as unstated for this product

type: task
state: ready
filed_at: 2026-09-04T22:00:54Z
version: 2
priority: 3
points: 2

## Description

Three of them are inherited from the fleet and unstated here: the provenance attestation on a published artefact, the lockfile policy for a development dependency, and the response window a reported vulnerability gets. SECURITY.md is where two of the three belong.

