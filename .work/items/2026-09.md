schema: 1

# field-edits: Field edits: one set command writes the dictionary, with one owner per field

type: story
state: done
filed_at: 2026-09-04T22:00:51Z
version: 9
priority: 1
points: 5
reviewer: abhijeet

## Description

set shipped in 02251fa and writes the field dictionary, and the design this story carried changed while it sat here: the carve that gave estimate and assign their own verbs was dropped, so set writes points, hours_estimate and assignee itself. writerOf in src/domain/fields.ts is the one statement of which command owns which field, and mark keeps severity and priority because both are audited with a reason. README's status table records estimate and assign as specified and not implemented, naming the set form that covers each.

## Acceptance criteria

- [x] set writes one stored field and refuses a field another command owns
- [x] set writes the fields the design gave estimate and assign, which is the departure this item records
- [x] each write reports the before and after pair the event log stores
- [x] a field that was already correct produces no set line and does not count

## Evidence

- commit 02251fa feat(cli): add set command and unify the description/desc field name (#24)

# relations: Relations: typed links, and the relation graph in the store

type: story
state: done
filed_at: 2026-09-04T22:00:51Z
version: 8
priority: 1
points: 8
reviewer: abhijeet

## Description

Shipped in 8d8d33d as relation add and relation remove, which are what the design called link and unlink. An edge is stored once as a Relations section on its source record and every inverse is derived on read, which ADR-0015 argues; blockersOf, the derived blocked flag, guards G2 and G7, DOR3 and the dep component of next all read that one graph. Three of the six kinds, caused_by, discovered_from and split_from, load and show and have no writer: ADR-0015 exposes a kind on an argued second caller rather than by default, so relation add writes blocks, duplicates and relates_to and refuses the rest by name.

## Acceptance criteria

- [x] a relation survives a round trip through the record grammar
- [x] blockersOf returns the active blockers of a stored item
- [x] G2 refuses a start that a blocker should refuse
- [x] the next ranking dep component is non-zero for an item that blocks another

## Evidence

- commit 8d8d33d feat(relation): add typed links between items with guards, doctor findings, and derived reads (#34)

# sprints-boards: Sprints as stored records, and the board as a projection over them

type: story
state: done
filed_at: 2026-09-04T22:00:51Z
version: 8
priority: 2
points: 13
reviewer: abhijeet

## Description

Both halves shipped and one of them shipped under a different answer than this item assumed. Sprints are stored records in one sprints.md (fbab9fb, ADR-0016), and status now prints sprint identity, day of sprint and committed items and points, so G4's sprint half is real. The board is not a stored entity: 4b2875d ships board as a projection of the backlog grouped by state, and ADR-0018 argues that it stores nothing, which leaves guards G3 and G4 disarmed because a work-in-progress limit per column and a board membership are workspace configuration rather than board state. That remainder is workspace-config, which already owns the board columns. absent_features, which this item cited as the symptom, was removed from status in 9888781 once the last of the four names was built.

## Acceptance criteria

- [x] a sprint is a stored record with a lifecycle, a committed set and a recorded carry-over
- [x] status prints sprint identity, day of sprint and the committed count and points
- [x] board groups the live work by state and scopes to the open sprint
- [x] the stored fact G3 and G4 need is recorded as workspace-config's, with ADR-0018 as the argument

## Evidence

- commit fbab9fb feat(sprint): add sprint lifecycle with commit tracking and carry-over (#35)
- commit 4b2875d feat(board): add board as a projection of the backlog by state (#37)

# undo: Undo: revert the most recent transaction by appending its inverse

type: story
state: ready
filed_at: 2026-09-04T22:00:52Z
version: 2
priority: 2
points: 5

## Description

Most recent in the workspace rather than yours, because a per-actor stack lets two people undo past each other. Not itself an undo, so repeated calls walk backwards. Appending rather than deleting, so the revert is in the history. Severe confirmation: the transaction id typed, or --confirm with it.

## Acceptance criteria

- [ ] repeated undo walks backwards rather than toggling
- [ ] a revert whose entities have moved since is refused and names the newer transaction
- [ ] the revert is itself a transaction in the history

# history: History, and resolving a transaction id a confirmation printed

type: story
state: draft
filed_at: 2026-09-04T22:00:52Z
version: 2
priority: 2
points: 3

## Description

The entity-scoped half shipped in 7ca95a0: history <id> lists every recorded change to one item, newest first, with the actor on each. R4's other half is history --txn, which resolves the transaction id every mutation prints back to the events it wrote, and no read verb resolves one today. That is what is left of this item.

# gate-command: Gate: print what the ready or done gate will decide before you run a transition

type: task
state: ready
filed_at: 2026-09-04T22:00:52Z
version: 3
priority: 3
points: 3

## Description

explain already prints the failing rules of both gates, and gate is the cheap single-gate form: one verdict, no record. The transition help page named it before this build and does not now, so nothing in the tool points a caller at a command that does not exist; explain is what a refusal names. This stays open because a verdict-only read is still the one shape the inventory lacks.

# workspace-config: Workspace configuration, and the precedence a flag beats

type: story
state: draft
filed_at: 2026-09-04T22:00:52Z
version: 2
priority: 2
points: 5

## Description

Four defaults are compiled in and should be workspace configuration: which types have a review step, which is guard G5's input and is currently story, bug and epic; the next weights; the point scale; and the board columns. ADR-0018 adds the two stored facts that arm the disarmed guards to this item's scope: a work-in-progress limit per column for G3, and a board membership for G4, neither of which the board itself stores. Precedence is flag, then environment, then workspace file, then built-in default, with the rendering deliberately excluded because DR5 refused an implicit input to it.

# doctor: Doctor, and the finding for a value shaped like a credential

type: story
state: draft
filed_at: 2026-09-04T22:00:52Z
version: 2
priority: 2
points: 8

## Description

doctor shipped and raises nine findings over the records, the event log, the relation graph and impediments. This item is the tenth: threat-model finding F10 has two halves and the other one is closed, because verbose logging reports a field by name and size and --log-values is the explicit opt-in for its content. What is left is a finding that flags a stored value matching a common secret shape, because a work-management tool is a place people paste things and the store is committed to git.

# metrics: Metrics: velocity, burndown and cumulative flow

type: story
state: draft
filed_at: 2026-09-04T22:00:53Z
version: 1
priority: 3
points: 13

## Description

Interface sections C.3 and C.4 give the layout algorithms and A.3 gives both budgets, which matter more here than anywhere else: cumulative_flow is 326 bytes and 211 Claude tokens, 1.55 bytes per token, so a byte budget alone mis-prices it by roughly two.

# human-layouts: The human rendering's board, charts and colour

type: story
state: draft
filed_at: 2026-09-04T22:00:53Z
version: 1
priority: 3
points: 8

## Description

This build ships a generic aligned projection with the global width rule and no colour at all. Interface section 4 gives byte-exact layouts and a six-assertion snapshot contract, one of which is the mechanical proof that colour carries no information: colour always output, with the escapes stripped, is byte-identical to colour never.

# export: Export to Markdown and CSV, with the formula guard

type: story
state: draft
filed_at: 2026-09-04T22:00:53Z
version: 1
priority: 4
points: 5

## Description

Threat-model finding F4: the CSV export is RFC 4180 quoted and not formula guarded, so a title beginning with an equals sign is a formula in a spreadsheet. RFC 4180 is the format and the guard is a prefix rule on top of it.

# hooks: Hooks, and the two exit codes the contract reserves for them

type: story
state: cancelled
filed_at: 2026-09-04T22:00:53Z
version: 2
priority: 4
points: 8
resolution: wont_do

## Description

DR6 makes a hook an external executable that can veto a proposal and never edit it. DR5 reserves HOOK_REFUSED at 3 and HOOK_FAILED at 1, and neither is in this build's closed set because nothing can produce one. Threat-model findings F1 and F7 are the consent gate and the path safety rule that land with it.

# generated-artefacts: Completions, the man page, and the adapter write-safety contract

type: task
state: ready
filed_at: 2026-09-04T22:00:53Z
version: 2
priority: 4
points: 5

## Description

All three generate from the command inventory that already backs help and the schemas. Threat-model finding F11 is the one new rule: an adapter write prints its diff, takes a moderate confirmation, backs up what it replaces, is idempotent on a re-run, and states the command that reverts it.

# packaging: A bundled dist entry so the package can be published

type: chore
state: done
filed_at: 2026-09-04T22:00:54Z
version: 6
priority: 4
points: 3

## Description

The bundle shipped in 01bce24: npm run build bundles src/cli/entry.ts to dist/treadle.js with esbuild, package.json points bin and files at that one file, and the build weighs it against the 512,000 byte budget bench/budgets.json records under DR8 and fails rather than warns when it is over. The last measured build is 347,048 bytes, 1.5x under. What is left is not packaging: package.json keeps private: true until the name clearance runs, which is one of the three interlocks docs/RELEASING.md carries and none of them is this chore's to open.

## Evidence

- commit 01bce24 build(release): add release automation and supply-chain CI gates (#8)

# supply-chain: Supply-chain controls F13 names as unstated for this product

type: task
state: done
filed_at: 2026-09-04T22:00:54Z
version: 5
priority: 3
points: 2

## Description

All three are stated now. ignore-scripts=true is in a committed .npmrc, the lockfile is committed and every workflow installs with npm ci, and the release path exports an SBOM and attests the tarball through GitHub's OIDC identity; test/architecture/supply-chain.test.ts is F13's enforcement and npm run licences refuses a licence off the allowlist. SECURITY.md carries the two that belong in a policy a reporter reads: the acknowledgement and assessment windows, and the supply-chain controls this repository holds itself to. One qualifier stays on record in docs/VERIFICATION.md: provenance at publish is asserted over the workflow and the preflight script, because no release has ever fired.

