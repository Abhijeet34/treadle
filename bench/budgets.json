{
  "tolerancePercent": 25,
  "derivedFrom": {
    "runId": "NOT MEASURED: no run has produced these limits yet",
    "date": "NOT MEASURED",
    "machine": "NOT MEASURED",
    "node": "NOT MEASURED",
    "note": "run `npm run bench -- --write-budgets` to fill the timing limits from a measured run"
  },
  "coldStartMs": null,
  "timing": {
    "enforced": false,
    "why": "the limits below were derived on one machine and have never been measured on a CI runner, so a failure there would be evidence about the runner rather than about the change; arm this by re-deriving on the runner with --write-budgets",
    "limits": {}
  },
  "axes": {
    "a1Durability": { "limit": 1, "enforced": true, "source": "axis A1", "why": "" },
    "a5SilentDrops": { "limit": 0, "enforced": false, "source": "axis A5", "why": "the store has never met this: a record heading renamed out of the grammar is dropped from every query with no finding" },
    "a5WholeStoreRefusals": { "limit": 0, "enforced": true, "source": "axis A5", "why": "" },
    "a5Crashes": { "limit": 0, "enforced": true, "source": "axis A5", "why": "" }
  },
  "absolute": {
    "peakRssReadKb": { "limit": 102400, "enforced": true, "source": "DR8, 100 MiB for a read at 50k" },
    "peakRssMutationKb": { "limit": 122880, "enforced": true, "source": "DR8, 120 MiB for a mutation at 50k" },
    "firstIndexBuildMs": { "limit": 6000, "enforced": true, "source": "DR8, 6 s for 50k items and 500k events" },
    "reindexAfterHandEditMs": { "limit": 50, "enforced": true, "source": "DR8, 50 ms after a hand edit of the largest shard" },
    "indexToTextRatio": { "limit": 1.0, "enforced": false, "source": "DR8, index at most 1.0x the text size", "why": "the landed index stores each record's and each event's source text, which the DR2 spike that measured 0.7x did not; the store owner has to decide whether the budget or the index changes" },
    "runtimeDependencies": { "limit": 0, "enforced": true, "source": "DR7, zero runtime dependencies" },
    "installUnpackedBytes": { "limit": 1572864, "enforced": true, "source": "DR8, 1.5 MB unpacked" },
    "bundleBytes": { "limit": 512000, "enforced": true, "source": "DR8, 500 KB bundle" }
  }
}
